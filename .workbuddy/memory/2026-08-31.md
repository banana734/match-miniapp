# 2026-08-31

## 全量代码扫描（只读，未改动任何文件）

项目：Match —— 四川大学华西临床医学院 × 计算机学院大创项目，微信小程序，
做「友导师 ↔ 家庭」的学业辅导 + 情绪陪伴双向匹配。

### 技术栈
- 前端：uni-app 3 + Vue 3 + Pinia 2，编译目标 mp-weixin。src/ 共 2775 行，
  11 个页面（login / role-first / home / match / message / My / notice /
  family-data / mentor-data / family-feedback / mentor-feedback）。
- 后端：Node 原生 http（无框架），server/ 共 2891 行，唯一依赖 mysql2。
  app.js 用 if-else 手写路由分发，同时托管 /admin 静态页。
- 数据库：MySQL，5 张表（users / role_bindings / trial_records /
  family_trial_feedback / mentor_trial_feedback）+ 4 个视图。
- 部署：Dockerfile + .github/workflows/docker-publish.yml；
  线上 API 基址 https://jymlbzjmwtbw.sealosbja.site/api（src/utils/api.js）。
- 已提交 5 个 commit，最新 60f3131（8.19.2）。

### 架构要点
- 身份一旦绑定即锁定：bindRole 与 saveProfile 双重校验 lockedRole，
  一个微信只能有一个身份。
- 登录降级：未配置 WECHAT_APP_ID/SECRET 时用前端 devClientId
  生成 dev-openid-xxx 回退登录，保证开发流程不断。
- 试课状态机：pending → formal（愿意）/ rejected（不愿意）/
  pending（需调整）/ removed（手动移除）。
- 资料存在 users.profile_json（JSON 列），靠 4 个 MySQL 视图用
  JSON_EXTRACT 摊平成列，供管理后台查询。
- useOrderedTagGroup 实现「按点击顺序排序」的多选标签（科目/困难/特质）。
- safeSwitchTab 防止重复切 tab 触发 timeout。

### 扫描发现的问题（尚未修复）
- **P0 全表重灌**：utils/unified-db.js 的 writeUnifiedDb = replaceDatabase，
  每次保存资料/试课都 DELETE 全表再全量 INSERT。并发下会丢数据。
- **P0 无鉴权**：所有 /api/* 只信前端传的 openid，token 后端从不校验；
  /admin 与 4 个 /api/admin/* 接口完全裸奔，暴露手机号、微信号。
- P1 死代码：server/routes/auth.js 从未被引用（app.js 用的是 auth-real.js）。
- P1 死分支：routes/match.js 回退到 db.familyPool / db.mentorPool，
  但 readDatabase 从不返回这两个字段，永远为空。
- P1 applyTrial 在一次请求里两次全量 readUnifiedDb（自身 + getPoolByViewerRole）。
- 备注：server/db/mysql.local.json 明文存 MySQL 密码，已被 .gitignore 排除。

## 重复代码清理（后端 + 数据库，未触碰任何 .vue / src）

用户明确要求：Vue 页面代码不能改，只处理后端与数据库层。净减 128 行。

### 已删除
- `server/routes/auth.js`（31 行）：旧的假登录实现，已被 auth-real.js 完全取代，
  全仓零 require。
- `server/db/view_queries.sql`（5 行）：只有两条注释式 SELECT，且已过时
  （缺 database.js 里的 ORDER BY 子句）。

### 已重构：server/app.js 路由分发 295 → 203 行
原来 6 个 POST handler 除业务函数名外**逐字符完全一致**（各 13 行），
4 个 admin GET handler 也完全相同。改为模块级路由表 getRoutes / postRoutes，
再用 `hasOwnProperty` 按「方法+路径」精确匹配。
注意点：必须同时校验 req.method，否则 POST 打到一个 GET 路由会被误匹配，
原来的 if 判断天然带了方法校验，重构时要保住这个语义。
`/admin` 与 `/admin/*` 静态文件处理（含路径穿越防护）保留在路由表外不动。

### 已重构：routes/match.js 死分支 + 重复读库
原 getPoolByViewerRole 末尾回退到 `db.familyPool / db.mentorPool`，
但 readDatabase 从不返回这两个字段（早期 JSON 文件版残留），永远走不到。
抽出新纯函数 `buildPoolFromUsers(users, role)`，getPoolByViewerRole 变成它的薄包装。
trial.js 的 applyTrial 改为直接 `buildPoolFromUsers(getUserRecords(db), role)`，
**每次试课申请少读一遍全库**（原来是 readUnifiedDb 后再 getPoolByViewerRole 又读一次）。

### 验证方式
node --check 全部 server/*.js 通过；脚本对比 git HEAD 版与新版的路由集合，
确认 7 条 GET + 6 条 POST 一条不丢不增；启动服务冒烟测试，
本地 MySQL 可用，实测 /api/match/list 双方身份、4 个 admin 接口、
profile/detail、trial/list 均返回正确数据；404 与 OPTIONS 预检行为不变。

### 仍存在但未改（需用户决定）
- **前端逻辑重复（用户禁止改 .vue，仅记录）**：
  - family-feedback.vue 与 mentor-feedback.vue 的 `<script setup>` 各 2795 字符，
    把 family/mentor 名字替换后**完全一致**，只有模板文案不同。
  - mentor-data.vue 完全没用项目已有的 `useProfileForm` / `useOrderedTagGroup`
    / `constants/profile-options.js`，全靠手写：本地重复声明 subjectOptions
    等选项数组（这就是 profile-options.js 里 mentorSubjectOptions 零引用的原因）、
    手写 toggle/order 三个有序标签组（约 90 行）、手写 syncFormFromProfile。
    family-data.vue 则是正常使用共享模块的。两个资料页写法已经分叉。
  - match.vue 与 message.vue：`list`/`previewList`/`pairLines`（11 行）、
    `openDetail`/`closeDetail`（4 行）逐字符相同；onShow 守卫骨架相同。
- **数据库 schema 重复**：family_trial_feedback 与 mentor_trial_feedback 两张表
  结构完全相同（25 行 ×2），两个 feedback view 也只差表名（23 行 ×2）。
  这是按角色分表的有意设计，改成单表 + role 字段能省掉一大半，但会影响
  admin.js 的查询和视图，属于设计变更。
- **server/db/views.sql 与 database.js 的 ensureViews() 内容重复**，
  且 views.sql 已过时（两个 feedback view 缺 ORDER BY）。建议删除，等确认。
- trial.js 内部「取活跃记录 → 排序 → 取最新」模式重复 3 次；
  profile.js 的 binding upsert 块在 bindRole 与 saveProfile 中各写一遍。

## 转本地开发（用户已停掉 Sealos 线上服务）

- 线上 https://jymlbzjmwtbw.sealosbja.site 已停用；src/utils/api.js 已改为
  `http://127.0.0.1:3000/api`。dist/dev 里残留的旧线上地址重编译即消失。
- **本地开发 3 步**：① `cd server && node app.js` 起后端（3000 端口）；
  ② 微信开发者工具 → 详情 → 本地设置 → 勾「不校验合法域名…」；
  ③ 重新编译。登录走 devClientId 回退，无需真实微信凭证。
- **数据库已确认全本地**：server/db/mysql.local.json → host localhost:3306,
  root, 库名 match；实测 MySQL 8.0.45, 机器名 LAPTOP-2ABE1O6T，
  5 表 + 4 视图齐全, users 18 条（全是 dev-openid-* 开发测试数据）。
- 微信 DevTools 报「合法域名校验出错」= 没勾本地设置的不校验域名选项，
  不是代码问题。

## 为 server 代码补全中文注释（逻辑零改动）

- 8 个核心 Node 文件全部补齐：文件头架构说明（职责、对应接口、状态机、
  目录导览）+ 每个函数的用途注释 + 关键行内注释。
  原有的「？？？」占位注释全部替换成真实说明。
- 验证方式：node --check 全过；剥离注释后与改动前对比逻辑一致
  （5 个未重构文件与 git HEAD 逐字符一致）；杀掉遗留测试进程后
  用注释版代码重启冒烟测试，7 条 GET + POST 登录 + 404 语义全部正常。
- 教训：Windows Git Bash 下 taskkill 的 //F 会被误转义报「无效参数」，
  要用 PowerShell 的 Stop-Process -Id <pid> -Force 杀进程。
- 另：端口 3000 有用户本地开发服务在跑（PID 57428），测试一律用 3999，
  不要动 3000。

## src JS 注释补全（逻辑零改动）

给 src/ 下全部 9 个 JS 文件补了中文注释（main.js / utils/api.js /
utils/navigation.js / store/user.js / 3 个 composable / 2 个 constants）。
Vue 页面文件按用户要求未动。验证方式：剥离注释后与 git HEAD 逐字符
对比 9/9 一致；ESM 语法检查全部通过。中途两次失误（user.js 重复
intro 键、误删原末尾 intro 字段）均已当场发现并修复，最终对比为证。
注意：用户自己把 api.js 的 API_BASE_URL 改成了 127.0.0.1:3000
（本地开发，Sealos 线上已停），此改动在 HEAD 之后、注释之前。

## Vue 页面 script 注释补全 + 删除 safeSwitchTab

用户要求给所有 .vue 的 `<script>` 段补中文注释（只动 script，不碰
template/style）。注意：此前用户曾禁止改 .vue，本次明确放开为「可加注释」。

- 删除 `src/utils/navigation.js` 的 `safeSwitchTab` 封装，并把 5 处调用
  （login / match / role-first / notice / message 的回首页）还原为原生
  `uni.switchTab({ url: '/pages/home/home' })`。判断依据：该封装的两重防护
  对当前项目属于过度封装——「已在目标页则拦截」永远走不到（调用点都在非首页），
  300ms 锁防的是极偶发的连点 timeout（仅控制台 warning，不影响功能）。
- 12 个 .vue 的 script 全部有注释。其中 6 个（App / home / My /
  两个 feedback / family-data）HEAD 里本来就带注释，本次编辑等同原内容、
  git 无变化；另 6 个（login / match / mentor-data / message / notice /
  role-first）真正写入了注释 + 删封装。
- 验证：剥离注释逐字符对比 git HEAD——7 个纯注释改动完全一致，5 个仅
  safeSwitchTab→uni.switchTab 那处不同；node --check 校验 12 个 script
  全部语法通过。中途修复一处手误（mentor-data.vue 一个函数结尾多写了 `)`）。
- 教训：Windows 下 `cd /d/VScode/Match` 在某些 Git Bash 调用里不可靠，
  用绝对路径 `/d/VScode/Match/...` 更稳；临时校验脚本放项目根会污染
  git status，用完即删。
- 所有改动均未提交，等用户 review。

## 精简重构（用户目标：代码好懂）

用户觉得代码太多看不懂，选择「精简重构成好懂结构」。方案按低风险优先，
最终落地 **A 前端去重 + C 删冗余 + 后端 P0 一并修**（B 合并反馈页不做，保持两文件）。

### A1 抽离展示工具 src/utils/display.js（新增）
把 match.vue / message.vue 里各抄一遍的 `list / previewList / pairLines / goHome`
抽到 display.js，两页改 `import { previewList, pairLines, goHome } from '@/utils/display'`，
删掉约 70 行复制。`list` 无直接调用，只导 previewList/pairLines/goHome，无死导入。

### A2 store 增 currentRole getter
src/store/user.js 加 `const currentRole = computed(() => role.value === 'mentor' ? 'mentor' : 'family')`
并导出。替换 4 个页面里散落的 7 处 `userStore.role === 'mentor' ? 'mentor' : 'family'`
（match 3 处 / message 2 处 / 两个 feedback 各 1 处）为 `userStore.currentRole`。
注意：页面里剩余的 `userStore.role === 'mentor'` 是纯布尔判断（如「是导师就显示家庭池」），
本就不是角色字符串映射，保留不动。

### C 删除死文件 server/db/views.sql
与 database.js 的 ensureViews() 内容重复，全仓零引用，已确认无影响后删除。

### P0 修复：全表重灌 → 按主键 UPSERT
server/db/database.js 的 `replaceDatabase`（DELETE 三表再全量 INSERT）改为
`upsertDatabase`：users 按 (openid,role)、role_bindings 按 openid、
trial_records 按 id 做 INSERT ... ON DUPLICATE KEY UPDATE（各自 4-5 个字段）。
unified-db.js 的 writeUnifiedDb 改调 upsertDatabase，文件头 P0 警告移除，
导出名同步改。database.js 文件头注释与 module.exports 同步更新。

验证（已全过）：
- 后端 node --check 全部 server/*.js 通过；replaceDatabase 全仓零残留引用。
- **P0 行为测试**（临时 `_dbtest.cjs` 跑本地 MySQL，用 upsert_test_* 用户，测完删）：
  ① insert 后 users 18→19（增量，不再清空全表）；
  ② 更新同一 openid 仍 1 行、profile.name 正确；
  ③ 并发两 upsert + 不传另两表 → **roleBindings/trialRecords 未清空**（仍 14/22），
     这是 P0 核心修复点，旧版并发会丢数据；④ 清理后库回到 18/14/22。
- 前端：display.js --check 通过；两页无遗留旧定义；currentRole 仅在 store 定义一处。

### 当前 git status（均未提交）
```
 M server/db/database.js
 D server/db/views.sql
 M server/utils/unified-db.js
 M src/pages/family-feedback/family-feedback.vue
 M src/pages/match/match.vue
 M src/pages/mentor-feedback/mentor-feedback.vue
 M src/pages/message/message.vue
 M src/store/user.js
?? src/utils/display.js
```
未解决项：后端 **无鉴权 P0**（/api/* 只信前端 openid、/admin 裸奔）仍待议，未动。

## 按「新手代码 / 标准规范 / 解耦合」风格重构

用户明确：重构别用高度抽象封装，尽量新手代码、标准规范、再解耦合。已存为
项目长期约定（MEMORY.md）。本轮落实：

### goHome 移回页面本地
- 从 `src/utils/display.js` 删除 `goHome`，改回 match.vue / message.vue 各自写
  一行本地 `const goHome = () => uni.switchTab({ url: '/pages/home/home' })`。
- 理由：goHome 是导航逻辑，不该塞在「display」展示文件里（违反解耦合 + 命名规范）；
  且本轮刚因「过度封装」删过 navigation.js，换壳放回自相矛盾。其余三页本就没用 goHome。
- 验证：display.js 语法 OK，两页本地定义 + 调用正常，无悬空引用。

### 两个 composable 改直白（保留共享形式）
- 重写 `useOrderedTagGroup.js` / `useTrialFeedbackPage.js`：注释更密、参数名更清楚
  （selectedList/allOptions）、去掉多余 otherValue 参数、保留原返回 API（调用方零改动）。
- **决策：保留共享，未完全拆散内联**。原因：useOrderedTagGroup 在 family-data.vue
  被用了 5 次（科目/困难/特质/风格/方式），完全内联会多出约 100 行重复、family-data.vue
  反而更难读；useTrialFeedbackPage 仅 2 页用，保留共享也避免两页逻辑漂移。
  已向用户说明此权衡，若用户坚持「每页自己写一份」的纯内联版可再改。
- 验证：两文件 node --check 通过；family-data 用的 order/selected/unselected/toggle
  返回名未变，调用方不受影响。

### 当前 git status（均未提交，本轮新增改动叠加在之前 A1/A2/C/P0 之上）
```
 M server/db/database.js
 D server/db/views.sql
 M server/utils/unified-db.js
 M src/store/user.js
 M src/utils/display.js            # 删 goHome
 M src/composables/useOrderedTagGroup.js
 M src/composables/useTrialFeedbackPage.js
 M src/pages/match/match.vue       # goHome 本地化
 M src/pages/message/message.vue   # goHome 本地化
 M src/pages/mentor-feedback/family-feedback.vue
 M src/pages/family-feedback/family-feedback.vue
?? src/utils/display.js            # 注意：此行应为 M（已追踪），实际是 ?? 因之前未提交
```
更正：display.js 是新建未提交文件，git 显示为 `??`。
