# 2026-09-16 工作日志

## 修复：切换测试账号弹层列表被截断（只显示友导师、家庭端看不到）
- 根因：微信小程序 `scroll-view` 必须有**固定高度**才能滚动。原代码用 `flex:1 + max-height` 撑不出高度，列表被直接剪断，家庭端账号看不到；右侧身份标签也因名字过长被挤出屏幕。
- 改动（仅 `src/pages/My/My.vue`）：
  - 滚动区 `.modal-list` 改为 `height: 56vh`（固定高度，可滚动）。
  - 账号列表由单一 `switchList` 拆成 `mentorItems` / `familyItems` 两个 ref，弹层里按「友导师 / 家庭」两组展示，各带人数标题（group-title）。
  - 横向内边距移到 `.switch-group` 上（避免 scroll-view 自身 padding 真机表现不一致）。
  - `switch-name` / `switch-sub` 加 `overflow:hidden; white-space:nowrap; text-overflow:ellipsis` 防溢出。
- 编译通过（dev + build 均含 group-title / switch-group）。dev watcher 已重启（task DGGuXY）。
- 备注：用户截图反馈"没有显示完"，即此截断问题。

## 背景：用户要"上帝视角"切换账号
- 用户希望切换账号时两端（友导师 + 家庭）任意账号都能切（当前已支持），并表达"上帝视角"诉求（含义待确认：可能是切换列表里看配对关系 / 上帝总览页）。本次先修截断，确保两端都能看到、能点。
- 之前已用 `isDev = process.env.NODE_ENV !== 'production'` 门控，生产包已剔除该入口。

## 完善：查看双方用户数据 - 卡片显示上课/试课对象（配对关系）
- 需求：在 dev-data 页每个卡片上直接看到这个人正在跟谁上课/试课（配对关系+对方名字），不用进详情。
- 数据模型：`trial_records` 一个配对 = 同一 `card_id`、role 相反的两行（家庭一行+导师一行），`status` 区分 trial（试课）/formal（正式上课）/terminated（已解除）。
- 后端：
  - `server/routes/admin.js` 新增 `getAdminPairings()`：自连接 `trial_records`（a join b on a.card_id=b.card_id and a.role!=b.role），取对方 openid/role/status/card_id，按现有风格返回 `{success,total,list}`；module.exports 导出。
  - `server/app.js`：require 解构加 `getAdminPairings`，GET 路由表加 `/api/admin/pairings`。
  - 重启后端：发现占 3000 端口的是**旧后端实例**（PID 1615，加载的是改路由前的 app.js），导致新路由 404；杀掉旧进程后重启新后端，curl `/api/admin/pairings` 返回 200（list 当前为空，因库里尚无配对）。
- 前端 `src/pages/dev-data/dev-data.vue`：
  - 新增 `nameMap`（两端 openid→名字，来自 mentors/families 列表）、`pairingMap`（openid→配对数组）、`pairingChips(item)` 算「上课/试课对象」展示（对方名字+身份+状态）。
  - 卡片模板在摘要与详情之间插入 `pairing-block`（「上课 / 试课对象」），按 status 用不同色 chip：trial=试课 / formal=正式上课 / terminated=已解除。
  - 加载时并行拉 `/api/admin/pairings` 一并填充。
- 编译：build + dev 产物均含 pairing 结构；dev watcher 重启（task 4HOQfE）。
- 经验：① 老后端进程常驻占端口导致"改了路由还 404"，排查要看实际是哪个进程在监听 3000；② uni build 产物被微信开发者工具文件锁时，先 rm -rf 对应 dist 子目录再 build。

- 【修正】上方"完善"段的"数据模型"描述有误：trial_records 实际是**单向记录**（openid=发起方、card_id=对方、role=发起方角色），并非"同 card_id 两行"。原 `getAdminPairings` 用 INNER JOIN 凑双向 → 接口 total:0 → 全显示"未配对"；且配对只存发起方一行，对方（家庭）看不到；card_id 带 `family-/mentor-` 前缀而卡片 openid 不带，名字查不到；试课/正式混在一个 block。
- 修复（本次）：
  - 后端 getAdminPairings 改为**直接 SELECT**（去掉 JOIN），返回 myOpenid/myRole/counterpartOpenid(=card_id)/counterpartRole/status；重启后端加载。
  - 前端 `pairingMap` **双向展开**（一条记录既 add 发起方→对方，也 add 对方→发起方）+ `stripRolePrefix` 去 `family-/mentor-` 前缀，使两端卡片都能看到彼此、名字能解析。
  - `pairingChips` 拆 `trial(pending)` / `formal` 两组；模板拆「试课对象」「正式上课对象」两个 pairing-group。
  - build + dev 产物均含新结构；dev watcher 重启（task DbUTat）。
  - 验证（node 复刻逻辑 + 真实接口数据）：导师 ey12ipnt 看到试课 ttt/111、正式 哈哈哈；家庭 j5d1adqh 作为"对方"看到正式 方法/卢智浩（双向生效、名字正确非"未知用户"）。

## 微调：dev-data 卡片配对展示改为「两行常驻，没有写无」
- 用户要求：试课对象 / 正式上课对象两组永远都显示（像截图那样），某一组没数据就写「无」，不再显示「未配对」。
- 改动（仅 `src/pages/dev-data/dev-data.vue` 模板）：去掉外层 v-if 和 v-else「未配对」分支，两组各自 v-if 判断，空时显示 `pairing-none` 样式的「无」。
- 编译通过（build + dev 产物均含 2 处「无」+ 分组结构），dev watcher 已重启（task SKQDMQ）。

## 修复：重置/切换账号后，资料表单还显示上一个人的资料
- 根因：`resetLoginState`（store/user.js）清了登录态和缓存，但**没清 `profile`（共用资料字段池）**。资料表单页（family-data/mentor-data）初始化时直接从 `userStore.profile` 取值，且 onShow 只在后端「有」资料时才回填覆盖——新账号查不到资料时表单就保持本地残留 → 重置后显示"未填写完成"但能看到上一个人的资料。
- 改动：
  - `src/store/user.js`：把空资料对象抽成 `createEmptyProfile()`（初始与重置共用）；`resetLoginState` 增加清空 profile + profileCancelled；新增 `clearProfile()` 导出。
  - `src/pages/My/My.vue`：`switchToAccount` 切换账号后异步请求 `/profile/detail?openid=&role=`——后端有资料就 `updateProfile + completeProfile` 回填，没有就 `clearProfile()` 清残留。
- 经验：本地 store 的 profile 是跨账号共享的内存池，任何"换身份"操作（重置/切换/重新登录）都要同步处理 profile；表单页只在后端有资料时覆盖，查不到时以本地为准，所以换身份必须主动清。
- 编译通过（build + dev 均含 clearProfile / profile/detail）。

## 修复（续）：重置账号后表单仍显示上一个人资料 —— 真正根因是模块级 form 只初始化一次
- 用户反馈上一轮修复无效。排查发现 store 的修复确实在产物里（store/user.js 含 clearProfile），真正根因另有其处：
- **根因**：mentor-data / family-data 的表单是**模块级 `reactive({...})`，从 `userStore.profile` 取值**。小程序页面模块整个 app 生命周期只执行一次，`reLaunch` 不会重新执行模块代码 → form 一直保留首次加载时的旧值。onShow 的后端回填只在「后端查得到资料」时才覆盖；重置后的新账号查不到 → 表单保持上一个人的旧值。
- **修复**：两个页面各加一个 `onLoad` 钩子（先 import onLoad），回调里用现成的 `syncFormFromProfile(userStore.profile)` / `applyProfileToForm(userStore.profile)` 重刷表单。onLoad 只在新建页面实例时触发（物理返回不触发、草稿不丢），且先于 onShow 的后端回填，顺序正确。
- 经验：**uni-app 小程序里，模块级从 store 初始化的响应式状态，在 reLaunch/重新登录后不会自动刷新；「每次新开页面需要最新值」的初始化必须放 onLoad，不能依赖模块顶层代码。**
- 编译通过（build + dev 两页 js 均含 onLoad）。

## 联系页分区排版改造（2026-09-16 晚）
- pages/message/message.vue：把「待试课」「正式上课」从「标题卡 + 独立列表」改为一张大底卡（section-panel，浅灰蓝底 #f7f9fc + 描边），标题 + 数量徽章在顶部，原样式 match-card 白卡嵌在内部 trial-list；空状态（还没有待试课/正式上课）也放进底卡内部居中。
- 删除冗余的「当前XX卡片数量」summary-text；样式追加在 message.vue 新增 scoped style（.card.section-panel 覆盖白底、.section-empty 居中）。逻辑零改动，未提交。

## 试课两端同步 bug 修复（2026-09-16 晚）
- 现象：友导师在匹配页点「进行试课」后，被申请的家长端联系页看不到该卡片。
- 根因：server/routes/trial.js 的 buildTrialListPayload 只返回「发起方」自己的记录（item.openid === openid）。被申请方（openid 在 card_id 前缀里）查不到。
- 修法：发起试课时除存「申请人记录」外，镜像一条「对方记录」（openid=对方、role=对方、cardId=申请人卡片、cardData=申请人卡片），对方用自己的身份即可查到；applyTrial/submitTrialFeedback/removeTrialRecord 都通过 getPartnerLocator + ensurePartnerRecord/syncPartnerRecord 同步两端状态。
- 新增辅助：parseCardId、getPartnerLocator、ensurePartnerRecord、syncPartnerRecord（都在 trial.js）。
- 已用真实账号端到端验证：导师发起→两端列表都出现对方卡片；移除→两端都 removed。重启后端（PID 51028→新进程）加载新代码，临时测试脚本已删。

## 「联系」tab 数字徽标（2026-09-16 深夜）
- 需求：联系页有任何变化（新增卡片/移除/转正式），底部导航「联系」挂红点+数字（变化数）。
- 实现：原布尔红点 hasUnreadTrialNotice+showTabBarRedDot 整体替换为数字徽标方案：
  - store/user.js：unreadMessageCount + seenMessageSignature（已读签名，存 match-message-seen-signature，包 {value} 对象以区分「没看过null」和「看过但为空''」）；buildMessageSignature（每卡 `id:pending|formal` 排序拼接）、countMessageChanges（双向 diff：新增/转区/消失各算1处）、syncMessageBadge（setTabBarBadge/removeTabBarBadge index:2；当前页是联系页则直接算已读）、markMessageViewed、refreshTrialLists（静默拉 /trial/list，供非联系页刷新用）。
  - setTrialLists 内自动 syncMessageBadge → 匹配页申请/移除、联系页拉取都会触发。
  - home.vue onShow 加 refreshTrialLists（profileCompleted 时）；match.vue onShow 原有 loadTrialState 已覆盖。
  - setLoginInfo/resetLoginState 清签名缓存（换账号已读状态隔离）。
  - message.vue：markTrialLessonViewed → markMessageViewed。
- 已知限制：无轮询，用户停在某个 tab 不动时不会主动刷新；切 tab/操作时会更新。首次使用（从没打开过联系页）不显示徽标。
- npm run build:mp-weixin 验证通过。未提交。

## 切账号后家庭端不显示红点（修复，2026-09-16 深夜）
- 现象：导师端选家庭发起试课→导师「联系」有红点；但用开发调试切到家庭端，「联系」没红点。
- 根因：src/store/user.js 的 countMessageChanges 有 `if (seen===null) return 0` 的特殊判断，本意是不打扰新用户。但切账号时 setLoginInfo 会把 seenMessageSignature 清成 null，于是被这条判断静音，家庭端永远算出 0 变化。
- 修法：删除该 null 拦截，让 null 当作「空已读」处理——切到家庭端后其当前卡片全部算新变化，正常挂数字徽标。空账号（0 张卡）依旧是 0、不打扰。
- npm run build:mp-weixin 验证通过。未提交。

## 「联系」红点逻辑重写为「按卡片/按账号已读」（2026-09-16 深夜）
- 用户反馈：之前「签名对比」方案有 bug——切账号时记成空已读，把该账号全部卡片都算成新变化，红点=卡片数量；而用户要的是「只看没看过的新消息」。
- 重写为 per-card / per-openid 已读模型：
  - seenCardsByOpenid: { [openid]: { "id:pending"|"id:formal": true } }，持久化键 match-message-seen。
  - countUnseen()：当前列表里不在已读集合的卡片数（看过的、被移除的都不计）。
  - unreadMessageCount 改为 computed(countUnseen)；syncMessageBadge 在「联系」页时调用 markMessageViewed 标记全部当前卡片已读。
  - 切账号/登录不再清空已读（按 openid 各存各的）；resetLoginState 整份清空。
- 语义：别人新选了你(新试课/状态变正式)而你没看过→有红点；自己看过→无红点；切账号互不污染。
- build 验证通过。未提交。

## 联系页顶部搜索筛选框（2026-09-16 晚）
- 需求：联系页顶部加搜索框，有字时按关键字过滤卡片（标题/副标题/预览字段/详情字段都可被搜到）；待试课、正式上课两个「框框」(section-panel) 始终保留，查不到时在框内显示「未找到与「xx」相关的待试课/正式上课」。
- 实现（src/pages/message/message.vue）：
  - 模板顶部加 search-bar（白卡片 + input + 清空✕），v-if=profileCompleted 与卡片同条件。
  - 新增 searchKeyword(ref) + onSearchInput(受控 :value+@input) + clearSearch。
  - getCardSearchText(item)：拼所有可见文本（title/subtitle/preview/details 的 label、value、items）；cardMatchesKeyword：大小写不敏感 includes，空关键字=全部命中。
  - filteredTrialList/filteredFormalList（computed 过滤）+ filteredTrialCount/filteredFormalCount；底卡 count-badge 改用过滤后数量；空状态区分 searchKeyword.trim() 有无。
  - 原 trialLessonList/FormalList/Count 保留（computed 依赖），模板改用过滤版。
- 行为：搜索只影响展示，红点/已读逻辑不受影响；两底卡不消失。npm run build:mp-weixin 验证通过。未提交。

## 搜索框从联系页迁移到匹配页（2026-09-16 晚）
- 用户发现搜索框该放匹配页（match）而非联系页（message），要求迁移。
- message.vue 完全还原：删除顶部 search-bar、filteredTrial/formal* 系列、getCardSearchText/cardMatchesKeyword/onSearchInput/clearSearch、search-* 样式；底卡恢复用原始 trialLessonList/formalClassList/Count。
- match.vue 新增搜索框（与 message 同款样式）：顶部 search-bar（v-if=profileCompleted，受控 :value+@input，✕清空）；visiblePool → 新增 filteredPool（在隐藏试课卡片基础上再按关键字过滤）；match-list v-for 改用 filteredPool；filteredPool 为空时显示「未找到与「xx」相关的匹配」（有搜索）或「暂时没有可匹配的卡片」（无搜索）；getCardSearchText 额外纳入 badge（匹配页特有）。match.vue 原本无局部 style，新增 <style scoped> 放 search-bar / section-empty。
- 行为一致：大小写不敏感、空关键字=不过滤、可搜名字/科目/各项信息。npm run build:mp-weixin 验证通过。未提交。

## 后台新增「试课 / 配对关系」页签（2026-09-16 深夜）
- 用户选 A：在现有 admin 后台（server/admin/，挂主后端 /admin）补配对关系可视化。
- 改动 3 文件：
  - dashboard.html：tab-row 加按钮「试课 / 配对关系」；panel 区加 #pairingPanel（含 #pairingStatus / #pairingTableWrap）。
  - dashboard.js：tabConfigs 加 pairings（endpoint /api/admin/pairings，custom:'pairings'，无 countId 不写统计卡）；state 加 pairings；新增 stripRolePrefix / buildPairingNameMap（用 family/mentor 资料把 openid 解析成「姓名(角色)」）/ pairingStatusMeta（pending=待试课/formal=正式上课/removed=已移除/terminated=已终止）/ pairingContinueMeta / renderPairings。renderPairings 把镜像双记录按「两 openid 排序拼接」去重合并成 1 对，状态优先保留非 removed；renderTabData / loadData 支持 custom 与无 countId 的 tab。
  - dashboard.css：加 .pairing-table / .cell-pairing / .pair-sep / .cell-status / .badge-{pending,formal,removed,unknown}。
- 验证：/admin 返回 200；/api/admin/pairings 50 条原始记录去重后 29 对（removed13/pending9/formal4/terminated3）。静态文件改动无需重启后端。未提交。
- 注：此后台仍无访问密码（选项 C 未选），上线前需加鉴权。

## 配对页签改成「按人分组三态」视图（2026-09-16 深夜）
- 用户看了配对页签后要求：以「人」为中心分导师/家长两组，每人一行三态——正式上课(和谁)/待试课(是谁)/未匹配，类似飞书多维表格的分组视图。
- 重写 renderPairings（server/admin/dashboard.js）：镜像记录去重(pairMap) → 双向挂到人(personMap) → summarizePerson（formal 优先 > pending > 其余 removed/terminated 都算未匹配）→ buildMembers（先导师/家长名单全员，再补配对中出现但不在名单的孤儿 openid）→ renderGroupTable 两组表（姓名/状态徽章/对象/是否愿意继续），分组标题带人数统计。
- 删掉 buildPairingNameMap/pairingStatusMeta，新增 personStateMeta/nameOf；CSS 删 .pair-sep，加 .pair-group/.pair-group-title，cell-pairing 缩到 200px。
- 真实数据验证：导师13人(正式2/待试4/未匹配7)、家长10人(正式3/待试3/未匹配4)；「方法」同时和 3 个家长 formal 正确多对象显示。静态文件无需重启后端。未提交。

## 配对页签改为五列视图（2026-09-16 深夜）
- 用户要求：列改成「姓名 / 正式上课 / 正式上课对象 / 待试课 / 待试课对象」，没有就写「无」，去掉「是否愿意继续」列。
- 改动 server/admin/dashboard.js：删除 personStateMeta/pairingContinueMeta（不再用）；summarizePerson 返回 {formals, pendings}（不再单一 state 优先）；buildMembers 的 m 结构改 formals/pendings；renderGroupTable 重写为五列表头，有则显示状态徽章、无则「无」；分组标题统计改 countFormal/countPending。dashboard.css 把 .pairing-table min-width 调到 880px 适配五列。
- 真实数据验证：导师13人(正式2/待试5)、家长10人(正式3/待试5)，同一人可同时有正式上课+待试课两类对象。静态文件无需重启后端。未提交。

## 反馈管理页签：按人分类 + 单条删除（2026-09-16 深夜）
- 用户需求：反馈可以很多次，展示时**按人分类**（这个人的试课反馈 / 日常反馈），每条反馈后面有删除按钮，点了这条就消失。
- 把原来「家长试课反馈」「导师试课反馈」两个平铺页签，合并替换成一个「反馈管理」页签（首次把**日常反馈**纳入后台，之前完全没展示）。
- 后端：
  - `routes/admin.js` 新增 `getAdminFeedbacks()`：4 张反馈表（family/mentor × trial/daily）UNION ALL，输出带 `role`(family|mentor) 与 `kind`(trial|daily)；日期/时长统一别名为 `event_date`/`event_duration`（trial 用 trial_date、daily 用 class_date），交给 normalizeRow 解析 `_json`/`_at`。
  - 新增 `deleteAdminFeedback({id, role, kind})`：按 `${role}-${kind}` 查**固定映射表**选表后 `DELETE ... WHERE id = ?`。表名不拼接外部输入，无注入。
  - `app.js`：GET 路由加 `/api/admin/feedbacks`，POST 路由加 `/api/admin/delete-feedback`。项目分发只支持 GET/POST，**没为删除新加 DELETE 方法**，沿用 POST 保持一致。
- 前端 `dashboard.{html,js,css}`：
  - html：2 张统计卡合并为「反馈总数」(#feedbackCount)；2 个页签合并为「反馈管理」；2 个 panel 合并为 #feedbackPanel。
  - js：删除 `feedbackColumns` 及 familyFeedback/mentorFeedback 配置，改为 `feedbacks`(custom:'feedbacks'，endpoint /api/admin/feedbacks)；新增 `renderFeedbacks()`（按 openid 聚合 → 姓名排序 → 每人一块，块内分「试课反馈 / 日常反馈」两组）与 `renderFeedbackItem()`（类型+角色徽章、日期、删除按钮、字段行）；删除用**事件委托**挂在 #feedbackTableWrap 上 + `window.confirm` 二次确认，成功后从 `state.feedbacks` 移除该条并重渲染、同步刷新计数卡。
  - css：加 `.fb-person/.fb-group-label/.fb-item/.fb-row/.fb-key/.fb-val` 与 `.badge-{trial,daily,family,mentor}`、`.delete-feedback-btn`。
- 验证：
  - `/api/admin/feedbacks` → 18 条（trial 12 + daily 6；family 6 + mentor 12；8 个提交人），字段含 id/openid/role/kind/event_date/continue_choice 等。
  - 删除链路端到端：插临时行 → 计数 1→2 → 调接口 → 2→1、临时行 0（临时行已清，数据仍是 18）。不存在的 id 幂等返回 200；非法角色返回 500「未知的反馈类型」。
  - JS 语法 `node --check` 三个文件全通过；脚本静态核对 16 个 getElementById id 全部存在于 html、4 个页签都有 tabConfigs 配置；curl `/admin`、`/admin/dashboard.js`、`/admin/dashboard.css` 确认线上资产已更新。
- **注意**：改了 `routes/admin.js` + `app.js` **必须重启后端**（本次杀掉旧 PID 7564 重启）；而 html/js/css 是每次请求 fs 读盘，**不用重启**。
- 未做：后台仍无鉴权/访问密码（P0 待办）；未装 agent-browser（需下 ~500MB Chromium），故未做浏览器截图验证，改为静态核对 + 接口端到端验证。

### 追加调整：身份提升为分类层级（用户看截图后要求）
- 用户原话："这个按身份来分类 不要写在反馈里面" —— 指反馈卡片里的「导师 / 家长」徽章不该塞在每条反馈里，身份应作为**分类**。
- 改法（`server/admin/dashboard.js` + `dashboard.css`，仍是静态文件、免重启）：
  - 层级改为三层：**身份大组（导师 / 家长）→ 人（姓名）→ 反馈类型（试课 / 日常）→ 单条反馈**。
  - 新增 `renderRoleGroup(role,title)`（按 role 过滤人、统计「N 人 · M 条反馈」、该身份无人则不渲染该组）与 `renderPersonBlock(person)`（原来内联的人块抽成函数）。
  - 人的 role 判定：一个 openid 只有一种身份，取 `list.some(fb => fb.role==='mentor') ? 'mentor' : 'family'`。
  - `renderFeedbackItem` 删除 roleBadge（身份不再出现在卡内），只留「试课反馈 / 日常反馈」类型徽章 + 日期 + 删除按钮。
  - CSS：删掉不再使用的 `.badge-family` / `.badge-mentor`，新增 `.fb-role-group` / `.fb-role-title`（左侧蓝色竖条做层级视觉）。
- 验证：复刻聚合逻辑跑真实数据 → 导师 4 人 12 条 + 家长 4 人 6 条 = 18 条，无遗漏无重复；`node --check` 通过；线上 dashboard.js/css 已确认是新版、roleBadge 计数为 0。

## 资料页签加「彻底删除」按钮（2026-09-16 深夜）
- 用户要求：在「导师资料 / 家长资料」行后面也加删除按钮。删除范围用 AskUserQuestion 确认，用户选 **彻底删除**。
- 语义（用户选定）：删 users 那一行 + role_bindings + 他相关的试课记录 + 他提交的反馈；配对页签里这个人也一起消失。
- 后端 `routes/admin.js` 新增 `deleteAdminProfile({openid, role})`：
  - `DELETE FROM users WHERE openid=? AND role=?`
  - `DELETE FROM role_bindings WHERE openid=?`
  - 试课记录**删两个方向**（关键）：`openid=?`（他发起的）+ `card_id=?`（`${role}-${openid}`，别人记录里指向他的镜像记录）。只删前者会在配对/联系页留下「用户 xxxx」孤儿卡。
  - 反馈：4 张表各扫一遍 `WHERE openid=?`（一个人只有一种身份，实际只命中自己角色那两张）。
  - 返回 `{success, openid, role, deleted:{users, roleBindings, trialRecords, feedbacks}}` 便于前端提示删了多少。
  - 表名来自代码内固定数组，不拼接外部输入。
  - `app.js` 新增 POST `/api/admin/delete-profile`。
- 前端 `dashboard.{js,css}`：
  - `renderTable(mountNode, columns, rows, actionRole)` 多一个可选参数，传了就在末尾加「操作」列（删除按钮带 data-openid/data-role/data-name）；新增 `rowDisplayName(row, role)` 取人名校验提示用。
  - family/mentor 的 tabConfigs 各加 `actionRole`；`renderTabData` 透传。
  - 新增 `handleDeleteProfile(button)`：confirm 弹窗把后果逐条列清（资料/身份绑定/试课记录/反馈 + 对方卡片也会消失 + 不可恢复）→ POST → alert 汇报各表删除条数 → 调 `loadData()` 整份重拉（因为资料/反馈/配对三个页签都受影响）。
  - `bindProfileDelete(wrapId)` 事件委托绑到 familyTableWrap / mentorTableWrap。
  - CSS：删除按钮样式与 `.delete-feedback-btn` 合并成共享选择器；`.cell-action` 做**右侧吸附**（`position:sticky; right:0`），因为资料表 min-width 1880px 要横向滚动，不吸附的话删除按钮在最右边看不见 —— 表头 th 背景 #f8fafc、td #fff、偶数行 #fbfcff 分别设背景，避免滚动时透出下层文字。
- 验证（端到端，造临时人）：
  - 造 users+role_bindings+两条 trial_records(正/反)+两条 feedback(mentor/family 各一) → 调接口 → 返回 `deleted:{users:1, roleBindings:1, trialRecords:2, feedbacks:2}`，7 张表全部回到原始值，临时行残留全为 0；临时数据已清干净。
  - `node --check` 全通过；线上 dashboard.js/css 已确认新版。
- 注意：**改了后端必须重启**（本次 18252 → 22664）；前端静态文件免重启。
- 遗留：后台仍无鉴权（P0）。另外「别人提交的、内容是关于被删者的反馈」会保留（用户选的是"他提交的反馈"），如需一并清掉要再加 card_id 方向的删除。

## 修复：真机调试报「后端未连接」（2026-09-16 深夜）
- 现象：微信开发者工具**真机调试**时页面提示「后端未连接」（文案来自 family-feedback / mentor-feedback / *-daily-feedback / family-data 等页的 catch 分支）。
- 根因：`src/utils/api.js` 里 `API_BASE_URL = 'http://127.0.0.1:3000/api'`。**真机上的 127.0.0.1 指向手机自己**，手机上没有后端 → 请求必失败。（模拟器没问题，因为模拟器跑在电脑上。）
- 排查过程（确认不是后端的锅）：
  - 后端监听 `0.0.0.0:3000`（PID 22664），不是只绑 127.0.0.1 → 局域网可达。
  - 电脑自测 `curl http://192.168.2.152:3000/api/admin/families` → HTTP 200。
  - 局域网 IP：WLAN = `192.168.2.152`（另有 VMware VMnet1/VMnet8 和 WSL 虚拟网卡，**别选错**）。
  - 防火墙：三个配置文件都启用；但已有精确的入站 Allow 规则 `node.exe → C:\Users\Banana\.workbuddy\binaries\node\versions\22.22.2\node.exe`（TCP、LocalPort Any、**Public** 配置文件），而 `Get-NetConnectionProfile` 显示 WLAN 类别正是 **Public** → 防火墙默认放行，不是瓶颈。
- 修法：`API_BASE_URL` 改成 `http://192.168.2.152:3000/api`，并把「真机必须用局域网 IP」的原因写进注释。
- 验证：dev watcher 自动重建，`dist/dev/mp-weixin/utils/api.js` 已变成 `192.168.2.152:3000`（23:52:26），且 `dist/dev` 里已无 `127.0.0.1:3000` 残留。
- 附带发现：`dist/build/mp-weixin` **不会**被 dev watcher 更新（还停在 22:09 的旧地址），跑发布版前要手动 `npm run build:mp-weixin`。
- 留给用户的动作：重新「真机调试」一次（会推送新代码）；确认手机和电脑在同一 Wi-Fi；确认「详情 → 本地设置 → 不校验合法域名」已勾选。换 Wi-Fi / 重启路由后 IP 可能变，要重新查并改这一行。
- 环境限制记录：本机 `wmic` / `netsh` 等系统级工具被安全策略禁用（wmic 直接报 SYSTEM TOOL DISABLED）；PowerShell 工具的 stdout 不回传，**绕法是把 PowerShell 结果写进 `$env:TEMP` 文件再用 Read 读**。

## 新增底部「反馈」tab（2026-09-17 凌晨）
- 需求：底部导航加一个「反馈」模块，展示**自己账号**的历史反馈，分「试课 / 日常」两块。
- 后端：新建 `server/routes/feedback.js`
  - `getMyFeedback(openid, role)`：按身份选 2 张表（family→family_*/mentor→mentor_*），返回 `{success, trial, daily}`。
  - 试课表日期列是 `trial_date`、日常表是 `class_date`，**共用一个 readFeedbackList({table, dateColumn, durationColumn, openid})**，统一别名为 `event_date`/`event_duration`，前端两段共用一套渲染。表名/列名都是代码内固定字面量，无注入。
  - 内含一份 `parseJsonField`/`normalizeRow`（与 admin.js 同思路；按用户「宁可少量重复」的风格没有强行抽公共模块，也没去动已跑通的 admin.js）。
  - `app.js`：require + GET `/api/feedback/list`，文件头目录导览补一行。
- 前端：新建 `src/pages/feedback/feedback.vue`
  - 两段用同一个模板渲染：`sections` computed 返回 `[{key,title,dateLabel,emptyText,list}]`，模板 `v-for sections` + 内层 `v-for section.list`，避免抄两份卡片结构。
  - `feedbackLines(item)` 把一条反馈拆成「满意点/客观不满意点/主观不满意点（胶囊）+ 是否继续合作（文本）」，并用 `list()`（来自 utils/display.js）清洗数组。
  - 状态徽章映射：pending=待试课(橙) / formal=已转正式、maintained=维持正式(绿) / rejected=未达成、terminated=已解除(红)；未知状态兜底「已提交」，不显示英文原文。
  - onShow 守卫与 message.vue 一致（未登录→login、未选身份→role-first），都满足才拉接口。
- `pages.json`：pages 数组注册 `pages/feedback/feedback`（标题「反馈」）；tabBar.list 插在「联系」和「我的」之间。
- **关键校验**：插入后「联系」仍在 **index 2**，所以 store 里 `setTabBarBadge({index:2})` 不需要改；但 `syncMessageBadge` 里的 `tabBarPages` 白名单**必须补上新页路由**，否则用户停在「反馈」tab 时徽标不刷新。已补。
- 验证：
  - `/api/feedback/list` 对家长账号（trial=2/daily=0）和导师账号（trial=2/daily=1）返回数量与库内 COUNT 完全一致；字段别名、JSON 解析成数组、updated_at 转 ISO 都正常；不传 openid 返回 `success:false`。
  - **dev watcher 自动完成了重建**（这次没有像以前那样需要重启编译进程）：`dist/dev/mp-weixin/app.json` 已含新页、tabBar 顺序正确（0首页/1匹配/2联系/3反馈/4我的），`pages/feedback/` 下 js/json/wxml/wxss 都已生成。→ 之前记忆里「改 pages.json 必须重启编译进程」这条**在当前 uni 版本下不成立**，dev watcher 能处理；但产物验证仍要 grep `app.json` 确认。
  - 后端改了要重启（本次 22664 → 51076）。
- 遗留：`dist/build/mp-weixin` 仍是旧的（发布版要手动 `npm run build:mp-weixin`）。后台仍无鉴权（P0）。
